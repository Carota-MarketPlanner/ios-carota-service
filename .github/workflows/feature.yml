name: Build And Test

on:
  pull_request:
    types:
      - edited
      - opened
      - synchronize
    branches: [ "main-b" ] # add branches

jobs:
  build:
    name: Build
    uses: ./.github/workflows/build.yml
    secrets: inherit

  # test: TO-DO: add test job